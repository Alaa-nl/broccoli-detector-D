# Python dependencies for the Broccoli Crown Detection backend.
# All versions are pinned so the team gets the same setup
# every time they run `pip install -r requirements.txt`.

# Web framework and server.
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9   # needed to accept file uploads

# Deep learning model (matches Deliverable B).
# PyTorch wheels. We use the plain (non-"+cpu") versions because the
# "+cpu" wheels are only published for Intel/x86_64, which fails to build
# on Apple Silicon (arm64). The plain wheels ship for both architectures,
# so pip picks the right one per machine (Intel Mac/Windows and ARM Macs).
# The --extra-index-url in the Dockerfile is harmless here.
ultralytics==8.2.103
torch==2.2.2
torchvision==0.17.2

# Image processing.
pillow==10.4.0
opencv-python-headless==4.10.0.84
numpy==1.26.4

# Data validation (used by FastAPI).
pydantic==2.9.2

# Monitoring: exposes operational + functional ML metrics on /metrics.
prometheus-client==0.20.0
